DEBIAN RELEASE
	edit debian/changelog (no deletes)
	dpkg-buildpackage
	TEST install (apt remove mint-yz-icons, GDebi install)
	check
	git status, push
	Grsync

MINT-YZ-ICONS NOTE:
If you added or removed color names, you need to update
~/mint-yz-icons/debian/postinst before running dpkg-buildpackage.
